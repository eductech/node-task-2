config:
  target: "http://localhost:4000"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Base
scenarios:
  - name: "Create user and check"
    flow:
      - post:
          url: "/login"
          json:
            login: "admin"
            password: "admin"
          capture:
            - json: "$.token"
              as: "authToken"
      - post:
          url: "/users"
          headers:
            Authorization: "Bearer {{ authToken }}"
          json:
            name: "TEST_USER"
            login: "test_user"
            password: "55w0rd"
      - get:
          headers:
            Authorization: "Bearer {{ authToken }}"
          url: "/users"